import notifier from "./notifier";

export default notifier;

// const type = notifier.notifierTypes;
// const thresMonNotifier = notifier.createNotifier(type.EMAIL);
// thresMonNotifier.setFailure({ serviceUrl: "abc" });
// thresMonNotifier.setFailure({ serviceUrl: "abc" });
// thresMonNotifier.setFailure({ serviceUrl: "abc" });
// thresMonNotifier.setFailure({ serviceUrl: "abc" });
